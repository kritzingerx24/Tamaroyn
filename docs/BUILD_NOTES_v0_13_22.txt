Tamaroyn v0.13.22 (HUD cleanup + compile fixes)

Fixes
- Fixed MapOverlay.gd compile error: Rect2 has no translated() in Godot 4.
- Removed DebugHud.gd warning by not shadowing global HudStyle class.
- Renamed DebugHud.gd _bp_make_row parameter from 'name' to 'label_text' to avoid Node.name shadow warning.

HUD visual polish
- Bottom chat/status strip: font sizing now derives from the project theme (more consistent across resolutions).
- Bottom tabs (Kills / Glimpse): text is vertically centered; hover highlight added for clickable tabs.
- Team counts (top-center): compacted spacing, reduced font size to better match the top edge bars.
